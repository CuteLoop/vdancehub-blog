# railway.toml (repo root)
[build]
builder = "NIXPACKS"
# Build CSS with Tailwind/PostCSS, then build Hugo into my-dance-blog/public
buildCommand = """
cd my-dance-blog
npm ci
npx tailwindcss -i assets/input.css -o static/css/main.css --postcss --minify
hugo --minify --baseURL ${BASE_URL:-/}
"""

[start]
# Serve the generated static site on $PORT
startCommand = "npx -y serve -s my-dance-blog/public -l ${PORT:-8080}"
